apiVersion: v1
Kind: pod
metadata:
  name: portfolio-pod
  labels:
    app: portfolio
spec:
  containers:
    -name: portfolio
    image: chaitanyagidijala123/portfolio:v1
    ports:
      -containerPort: 80
